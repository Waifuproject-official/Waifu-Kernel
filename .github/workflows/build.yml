name: Release

on:
  push:
    branches: [ "surya" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@main
      
      - name: Run build script
        run: |
          source build.sh